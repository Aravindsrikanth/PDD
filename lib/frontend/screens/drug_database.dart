import 'package:flutter/material.dart';

class DrugDatabaseScreen extends StatelessWidget {
  const DrugDatabaseScreen({super.key});
  @override
  Widget build(BuildContext context) {
    return Scaffold(appBar: AppBar(title: const Text('Drug DB')), body: const Center(child: Text('Drug Database Ready')));
  }
}
